---
- hosts: all
  become: yes
  pre_tasks:
    - name: Update packages
      apt:
        autoclean: yes
        autoremove: yes
        update_cache: yes
        upgrade: yes
  roles:
    - { role: common, dir: '/app' }
